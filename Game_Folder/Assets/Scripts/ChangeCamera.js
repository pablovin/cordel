#pragma strict

import UnityEngine;

function Start () {

    /*
     Camera.main.gameObject.camera.orthographicSize = 100;         
     
     */
}

function Update () {
/*
    var theCamera = Camera.main.gameObject;    
    
    
	if(Input.GetKey("w"))
    {       
	     	theCamera.transform.Translate(Vector3(0,5,0));	     
	}
	else if(Input.GetKey("a"))
	{     	 
	     theCamera.transform.Translate(Vector3(-5,0,0));
	}
	else if(Input.GetKey("d"))
	{     	 
	     theCamera.transform.Translate(Vector3(5,0,0));
	}
	else if(Input.GetKey("s"))
	{     	 
	     theCamera.transform.Translate(Vector3(0,-5,0));
	}
*/
}